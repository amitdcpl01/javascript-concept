const drink = {
  color: 'brown',
  carbonated: true,
  sugar: 40
};

type Drink = [string, boolean, number];

const pepsi: Drink = ['brown', true, 40];
const spriite: Drink = ['clear', true, 30];
const tea: Drink = ['brown', false, 0];

const carSpecs: [number, number] = [400, 3322];

const carStats = {
  horsepower: 400,
  weight: 3354
};
